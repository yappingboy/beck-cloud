# kube-node-lease

**Purpose:** Node lifecycle management.

**What it does:** A standard Kubernetes namespace that stores lease objects for node membership and scheduling. It has no applications. Its purpose is to hold the Lease resources that K3s uses for cluster membership coordination.
