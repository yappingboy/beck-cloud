# gridspace

**Purpose:** 3D design and CAM software.

**What it does:** Runs Griddspace (spelled "gridspace" in Kubernetes), a web-based 3D CAD/CAM platform for creating and editing meshes, generating toolpaths, and exporting fabrication files. The namespace contains the application deployment and its service endpoint, providing a dedicated space for all design workloads.
