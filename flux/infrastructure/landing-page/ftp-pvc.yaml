apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ftp-data
  namespace: landing
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: local-path
