apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tdarr
  namespace: tdarr
spec:
  secretName: tdarr-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - tdarr.becklab.cloud