apiVersion: v1
kind: Secret
metadata:
  name: tdarr-secrets
  namespace: tdarr
stringData:
  # Placeholder for TDarr API key or other sensitive configuration
  api_key: "PLACEHOLDER_TDARR_API_KEY"
  # Placeholder for any required DB credentials if using external DB
  db_password: "PLACEHOLDER_DB_PASSWORD"