apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - llamactl-service.yaml
  # rho: disabled — @rhobot-dev/rho npm package not yet published; init container
  # crashes trying to install it. Re-enable once the package exists or the image is
  # replaced with a pre-built one.
  # - rho.yaml
