# Auto-split from cluster files
apiVersion: v1
kind: Service
metadata:
  name: swiparr
  namespace: media
spec:
  selector:
    app: swiparr
  ports:
  - name: http
    port: 4321
    targetPort: 4321
    protocol: TCP
  type: ClusterIP
