# Auto-split from cluster files
apiVersion: v1
kind: Service
metadata:
  name: tdarr
  namespace: media
spec:
  selector:
    app: tdarr
  ports:
  - name: http
    port: 8265
    targetPort: 8265
    protocol: TCP
  type: ClusterIP
