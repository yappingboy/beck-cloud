apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: qbit
  namespace: media
spec:
  secretName: qbit-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - qbittorrent.becklab.cloud
