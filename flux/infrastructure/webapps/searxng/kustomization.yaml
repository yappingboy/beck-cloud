apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: webapps

resources:
  - certificate.yaml
  - secret-sops.yaml
  - configmap.yaml
  - deployment.yaml
  - ingress.yaml

images:
  - name: searxng/searxng
    newName: docker.io/searxng/searxng
    newTag: latest
