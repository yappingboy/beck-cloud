apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: searxng-tls
  namespace: webapps
spec:
  secretName: searxng-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - searx.becklab.cloud
