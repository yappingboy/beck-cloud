apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: landing-page
  namespace: webapps
spec:
  secretName: landing-page-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - becklab.cloud
