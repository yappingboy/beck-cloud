apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tooljet-tls
  namespace: webapps
spec:
  secretName: tooljet-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - tooljet.becklab.cloud
