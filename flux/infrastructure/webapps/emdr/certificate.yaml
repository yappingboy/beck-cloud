apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: emdr-tls
  namespace: webapps
spec:
  secretName: emdr-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - emdr.becklab.cloud
