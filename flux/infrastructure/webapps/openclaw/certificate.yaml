apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nova-tls
  namespace: webapps
spec:
  dnsNames:
    - nova.becklab.cloud
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: nova-tls
