apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: security

resources:
  - namespace.yaml
  - wazuh/wazuh-values.yaml
  # falco: deferred — ebpf/modern_ebpf drivers crash under KVM/Cilium.
  # Revisit with kmod driver + matching kernel headers, or layer Tetragon later.
  # suricata: deferred until Phase 3 (after Wazuh foundation is stable)
  # trivy-operator: deferred until Phase 2
