apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: security

resources:
  - namespace.yaml
  # wazuh: disabled — helm repo unavailable and resource requirements (4GB+) exceed homelab capacity
  # re-enable once a dedicated node or separate namespace with higher limits is available
  - falco.yaml


