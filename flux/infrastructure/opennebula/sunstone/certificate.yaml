apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opennebula
  namespace: opennebula
spec:
  secretName: opennebula-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - one.becklab.cloud
