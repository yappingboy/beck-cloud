# gridspace/gridspace-app

**Purpose:** Grid-based app platform

**What it does:** Gridspace application with deployment, PVC, service, ingress routes, middleware, and TLS certificates.

**Services:**
- `grid-apps` — Deployment, PVC, Service
- `ingressroutes` — IngressRoute, Middleware
- `certificates` — TLS Certificate
