apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sources
  - controllers
  - configs
  - csi-snapshotter
  - traefik
  - identity
  - security
  - media
  # velero: removed — Flux GitOps (all state in git) + ZFS snapshots (data layer)
  #         replace it. Re-add if off-cluster DR becomes a requirement.
  - llm
  # rancher: removed — K3s + Flux replaces Rancher's management role;
  #          files kept in flux/infrastructure/rancher/ for reference
