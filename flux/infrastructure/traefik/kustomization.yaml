apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yaml
  - helmrelease.yaml
  - middlewares.yaml
  - dashboard-ingress.yaml
  - dashboard-certificate.yaml
