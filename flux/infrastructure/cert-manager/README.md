# cert-manager

**Purpose:** TLS certificate management for the cluster

**What it does:** Deploys cert-manager via HelmRelease and manages Let's Encrypt cluster-issuer (via cert-manager-config). Handles certificate provisioning for all TLS-enabled services.

**Special resources:** None — purely infra.
