apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cert-manager

resources:
  - namespace.yaml
  - helmrelease.yaml
  - clusterissuer.yaml

helmReleases:
  - name: cert-manager
    chart:
      repoURL: https://charts.jetstack.io
      version: 1.16.6
      interval: 1m
