apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: micro

resources:
  - namespace.yaml
  - resource-quota.yaml
  - pvc.yaml
  - certificate.yaml
  - ghcr-credentials.yaml
  - micro-app
