# micro

**Purpose:** Microservices platform

**What it does:** Hosts auth-service with RBAC (Role, RoleBinding, ServiceAccount), TLS certificates, ingress routes, and middlewares. Includes ghcr.io pull credentials.

**Special resources:** PVC for auth-service data, ResourceQuota for resource management.
