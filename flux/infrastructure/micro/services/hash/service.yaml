# Auto-split from cluster files
apiVersion: v1
kind: Service
metadata:
  name: hasher
  namespace: micro
spec:
  selector:
    app: hasher
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 8080
    protocol: TCP
