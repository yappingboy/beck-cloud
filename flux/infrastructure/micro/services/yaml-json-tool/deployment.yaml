apiVersion: apps/v1
kind: Deployment
metadata:
  name: yaml-json-tool
  namespace: micro
  labels:
    app: yaml-json-tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yaml-json-tool
  template:
    metadata:
      labels:
        app: yaml-json-tool
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: yaml-json-tool
          image: ghcr.io/yappingboy/micro/yaml-json-tool:v1.0.0
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: yaml-json-tool
  namespace: micro
spec:
  selector:
    app: yaml-json-tool
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
