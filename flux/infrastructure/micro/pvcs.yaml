# Auto-split from cluster files
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: micro-storage
  namespace: micro
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
