# cert-manager-config

**Purpose:** Cert-manager configuration resources

**What it does:** Defines the Let's Encrypt production ClusterIssuer used by all TLS services across the cluster.

**Special resources:** None — purely infra.
