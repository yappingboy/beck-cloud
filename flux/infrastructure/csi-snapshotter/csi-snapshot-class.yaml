# VolumeSnapshotClass intentionally empty: Rook/Ceph was removed from this deployment.
# ZFS snapshots + Velero (if re-added) are used for data protection instead.
# If a CSI driver is added in future, define a VolumeSnapshotClass here.
