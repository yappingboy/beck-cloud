# identity/oauth2-proxy-media

**Purpose:** OAuth2 proxy for media SSO

**What it does:** Separate oauth2-proxy HelmRelease for media tier (requires /media group in LLDAP).

**Services:**
- `oauth2-proxy-media` — HelmRelease
