apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: identity

resources:
  - namespace.yaml
  - secret-lldap
  - secret-keycloak
  - redis
  - lldap
  - keycloak
  - oauth2-proxy
  - oauth2-proxy-media
  - logout-page
  - sso-redirect
  - sso-middlewares
  - email
  - pvcs.yaml
