apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: identity

resources:
  - namespace.yaml
  - pvcs.yaml
  - email
  - keycloak
  - lldap
  - oauth2proxy
  - redis
  - sso
#  - audit-sync
#  - logout-page
#  - rbac
#  - role-enforcer
  
