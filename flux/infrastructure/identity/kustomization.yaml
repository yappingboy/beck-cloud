apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: identity

resources:
  - namespace.yaml
  - audit-sync
  - email
  - keycloak
  - lldap
  - logout-page
  - oauth2poxy
  - rbac
  - redis
  - role-enforcer
  - sso
  