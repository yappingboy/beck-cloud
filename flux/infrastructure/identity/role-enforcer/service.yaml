apiVersion: v1
kind: Service
metadata:
  name: role-enforcer
  namespace: identity
  labels:
    app: role-enforcer
spec:
  selector:
    app: role-enforcer
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
