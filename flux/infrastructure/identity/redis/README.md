# identity/redis

**Purpose:** Redis session store

**What it does:** Redis StatefulSet + Service with secret. Used for Keycloak session management.

**Services:**
- `redis` — StatefulSet, Service, Secret
