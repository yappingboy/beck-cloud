apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lldap
  namespace: identity
spec:
  secretName: lldap-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - lldap.becklab.cloud
