apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secrets
  namespace: identity
stringData:
  # client-secret: from Keycloak Admin > Clients > oauth2-proxy > Credentials tab
  client-secret: CHANGE_ME
  # cookie-secret: generate with: openssl rand -base64 32
  cookie-secret: CHANGE_ME
