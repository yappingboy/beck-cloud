apiVersion: v1
kind: Secret
metadata:
  name: audit-sync-secrets
  namespace: identity
  labels:
    app: audit-sync
type: Opaque
stringData:
  KEYCLOAK_ADMIN_PASSWORD: "<from secret-keycloak.yaml>"
  KEYCLOAK_SERVICE_CLIENT_SECRET: "<generate: openssl rand -hex 32>"
  DIRECTUS_TOKEN: "<admin token or service account token>"