# identity/oauth2-proxy

**Purpose:** OAuth2 proxy for admin SSO

**What it does:** oauth2-proxy HelmRelease for admin tier (requires /admins group in LLDAP). Includes secret.

**Services:**
- `oauth2-proxy` — HelmRelease, Secret
