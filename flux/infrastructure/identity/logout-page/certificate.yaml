apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: logout-page
  namespace: identity
spec:
  secretName: logout-page-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - logout.becklab.cloud
