apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: manyfold
  namespace: 3dprinting
spec:
  secretName: manyfold-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - manyfold.becklab.cloud
