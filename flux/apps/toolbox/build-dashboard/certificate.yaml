apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: build-dashboard
  namespace: toolbox
spec:
  secretName: build-dashboard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - builds.becklab.cloud
