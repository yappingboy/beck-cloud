apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mc-world
  namespace: minecraft
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
