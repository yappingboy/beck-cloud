apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: user-invite
  namespace: apps
spec:
  secretName: user-invite-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - user-invite.becklab.cloud
