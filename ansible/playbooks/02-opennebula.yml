---
# 02-opennebula.yml — Configure OpenNebula 7.2 AIO (frontend + KVM node)
#
# AlmaLinux 9 — OpenNebula CE 7.2 packages are PRE-INSTALLED by the CE ISO.
# oned, Sunstone, and FireEdge are already running.
#
# What lives here:
#   • Set oneadmin API credentials (/var/lib/one/.one/one_auth)
#   • Set up oneadmin SSH keypair (AIO passwordless SSH to itself)
#   • Add oneadmin to kvm and qemu groups
#   • Create NetworkManager bridge one0 (192.168.100.1/24) via nmcli
#   • Enable IP forwarding + firewalld masquerade for VM subnet
#   • Ensure OpenNebula services are started
#   • Add localhost as KVM host; create k3s-net virtual network
#
# Run: ansible-playbook -i inventory/hosts.yml playbooks/02-opennebula.yml

- name: Configure OpenNebula 7.2 AIO
  hosts: hypervisor
  become: true
  gather_facts: true

  tasks:
    # ── oneadmin credentials ───────────────────────────────────────────────
    - name: Ensure oneadmin .one directory exists
      ansible.builtin.file:
        path: /var/lib/one/.one
        state: directory
        owner: oneadmin
        group: oneadmin
        mode: '0700'

    - name: Check if oneadmin API auth file already exists
      ansible.builtin.stat:
        path: /var/lib/one/.one/one_auth
      register: one_auth_stat

    # The CE ISO sets its own oneadmin password during install.
    # Only write this file if it doesn't exist yet — never overwrite
    # a CE-provisioned password (it won't match the DB and breaks CLI auth).
    # To change the password post-install, use: oneuser passwd 0 <newpass>
    - name: Write oneadmin API auth file (only if not set by CE ISO)
      ansible.builtin.copy:
        content: "oneadmin:{{ one_admin_password }}"
        dest: /var/lib/one/.one/one_auth
        owner: oneadmin
        group: oneadmin
        mode: '0600'
      when: not one_auth_stat.stat.exists

    # ── oneadmin SSH keypair (AIO: frontend SSHes to itself) ───────────────
    - name: Check if oneadmin SSH keypair exists
      ansible.builtin.stat:
        path: /var/lib/one/.ssh/id_rsa
      register: oneadmin_ssh_key

    - name: Generate oneadmin SSH keypair
      ansible.builtin.shell: |
        su oneadmin -s /bin/bash -c "ssh-keygen -t rsa -N '' -f /var/lib/one/.ssh/id_rsa"
      when: not oneadmin_ssh_key.stat.exists
      args:
        creates: /var/lib/one/.ssh/id_rsa

    - name: Append oneadmin public key to authorized_keys
      ansible.builtin.shell: |
        cat /var/lib/one/.ssh/id_rsa.pub >> /var/lib/one/.ssh/authorized_keys
        chmod 600 /var/lib/one/.ssh/authorized_keys
        chown oneadmin:oneadmin /var/lib/one/.ssh/authorized_keys
      when: not oneadmin_ssh_key.stat.exists
      changed_when: true

    - name: Populate oneadmin known_hosts (localhost, IP, and AIO hostname)
      ansible.builtin.shell: |
        set -e
        KNOWN=/var/lib/one/.ssh/known_hosts
        for target in localhost 127.0.0.1 "{{ homelab_hostname }}" "{{ homelab_ip }}"; do
          ssh-keyscan -H "$target" >> "$KNOWN" 2>/dev/null || true
        done
        sort -u "$KNOWN" -o "$KNOWN"
        chown oneadmin:oneadmin "$KNOWN"
        chmod 600 "$KNOWN"
      changed_when: true

    # ── oneadmin group membership ──────────────────────────────────────────
    - name: Add oneadmin to kvm and qemu groups
      ansible.builtin.user:
        name: oneadmin
        groups: kvm,qemu
        append: true

    # ── Internal bridge (one0) — isolated, NAT masqueraded ────────────────
    # All VMs get a NIC here. Workers use this for kubelet / control-plane
    # traffic. The hypervisor masquerades this subnet to the internet so
    # workers can reach package repos and container registries.
    - name: Check if one0 bridge already exists
      ansible.builtin.command: nmcli con show one0
      register: one0_check
      failed_when: false
      changed_when: false

    - name: Create one0 internal bridge
      ansible.builtin.command: >
        nmcli con add type bridge con-name one0 ifname one0
        bridge.stp no autoconnect yes
      when: one0_check.rc != 0

    - name: Set static IP on one0
      ansible.builtin.command: >
        nmcli con modify one0
        ipv4.method manual
        ipv4.addresses "{{ one_int_gateway }}/24"
        ipv4.gateway ""
        ipv4.dns ""
      when: one0_check.rc != 0

    - name: Bring up one0
      ansible.builtin.command: nmcli con up one0
      when: one0_check.rc != 0

    # ── External bridge (br0) — bridged over physical NIC, direct LAN ─────
    # Only the k3s-server gets a NIC here. Workers reach the internet via
    # the hypervisor's masquerade on one0 instead.
    #
    # WARNING: reconfigures the active NIC. Atomically executed via nmcli;
    # expect a 1-3s blip. Override the detected NIC with:
    #   -e one_physical_nic_override=ens3
    - name: Detect physical NIC from default route
      ansible.builtin.shell: |
        ip route show default | awk '/default/ {print $5; exit}'
      register: physical_nic_detected
      changed_when: false

    - name: Set physical NIC fact
      ansible.builtin.set_fact:
        one_physical_nic: "{{ one_physical_nic_override | default(physical_nic_detected.stdout | trim) }}"

    - name: Check if br0 external bridge already exists
      ansible.builtin.command: nmcli con show br0
      register: br0_check
      failed_when: false
      changed_when: false

    - name: Create br0 external bridge over physical NIC
      ansible.builtin.shell: |
        set -e
        NIC="{{ one_physical_nic }}"
        NIC_CONN=$(nmcli -t -f NAME,DEVICE con show --active | awk -F: -v d="$NIC" '$2==d {print $1; exit}')
        NIC_ADDR=$(nmcli -t -f IP4.ADDRESS con show "$NIC_CONN" | cut -d: -f2 | head -1)
        NIC_GW=$(nmcli   -t -f IP4.GATEWAY con show "$NIC_CONN" | cut -d: -f2 | head -1)
        NIC_DNS=$(nmcli  -t -f IP4.DNS     con show "$NIC_CONN" | cut -d: -f2 | tr '\n' ' ')
        nmcli con add type bridge       con-name br0       ifname br0 \
          bridge.stp no autoconnect yes \
          ipv4.method manual \
          ipv4.addresses "$NIC_ADDR" \
          ipv4.gateway   "$NIC_GW" \
          ipv4.dns       "${NIC_DNS:-8.8.8.8}"
        nmcli con add type bridge-slave con-name br0-slave ifname "$NIC" \
          master br0 autoconnect yes
        nmcli con down "$NIC_CONN" || true
        nmcli con up   br0-slave
        nmcli con up   br0
      when: br0_check.rc != 0

    # ── IP forwarding + NAT for internal VMs ──────────────────────────────
    - name: Enable and start firewalld
      ansible.builtin.systemd:
        name: firewalld
        enabled: true
        state: started

    - name: Enable IP forwarding via sysctl
      ansible.posix.sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        sysctl_set: true
        state: present
        reload: true

    - name: Masquerade one0 traffic so workers reach the internet
      ansible.builtin.command: firewall-cmd --permanent --zone=public --add-masquerade
      changed_when: true
      failed_when: false

    - name: Allow FORWARD from one0 (worker outbound)
      ansible.builtin.command: >
        firewall-cmd --permanent --direct
        --add-rule ipv4 filter FORWARD 0 -i one0 -j ACCEPT
      changed_when: true
      failed_when: false

    - name: Allow FORWARD to one0 (return traffic)
      ansible.builtin.command: >
        firewall-cmd --permanent --direct
        --add-rule ipv4 filter FORWARD 0 -o one0 -j ACCEPT
      changed_when: true
      failed_when: false

    - name: Reload firewalld
      ansible.builtin.command: firewall-cmd --reload
      changed_when: true

    # ── OpenNebula services ────────────────────────────────────────────────
    - name: Ensure OpenNebula services are started
      ansible.builtin.systemd:
        name: "{{ item }}"
        state: started
        enabled: true
      loop:
        - opennebula
        - opennebula-fireedge

    - name: Wait for OpenNebula API (port 2633) to be open
      ansible.builtin.wait_for:
        host: localhost
        port: 2633
        delay: 5
        timeout: 60

    # ── Add KVM host (AIO: localhost) ──────────────────────────────────────
    - name: Check if KVM host already registered
      ansible.builtin.command: onehost show {{ homelab_hostname }}
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      register: onehost_check
      failed_when: false
      changed_when: false

    - name: Add localhost as KVM node
      ansible.builtin.command: onehost create {{ homelab_hostname }} -i kvm -v kvm
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      when: onehost_check.rc != 0

    - name: Wait for KVM host to reach MONITORED state (up to 120s)
      ansible.builtin.shell: |
        onehost list --no-header | grep {{ homelab_hostname }} | grep -w MONITORED
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      register: onehost_monitored
      retries: 12
      delay: 10
      until: onehost_monitored.rc == 0
      changed_when: false
      failed_when: false
      when: onehost_check.rc != 0

    # ── Virtual networks ───────────────────────────────────────────────────
    - name: Check if k3s-int vnet already exists
      ansible.builtin.command: onevnet show k3s-int
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      register: k3s_int_check
      failed_when: false
      changed_when: false

    - name: Write k3s-int vnet template (internal, isolated, NAT)
      ansible.builtin.copy:
        content: |
          NAME          = "k3s-int"
          VN_MAD        = "bridge"
          BRIDGE        = "{{ one_int_bridge }}"
          DNS           = "{{ one_dns_servers | join(' ') }}"
          GATEWAY       = "{{ one_int_gateway }}"
          NETWORK_MASK  = "255.255.255.0"
          AR = [
            TYPE = "IP4",
            IP   = "{{ one_int_ar_start }}",
            SIZE = "20"
          ]
        dest: /tmp/k3s-int.one
        mode: '0644'
      when: k3s_int_check.rc != 0

    - name: Create k3s-int virtual network
      ansible.builtin.command: onevnet create /tmp/k3s-int.one
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      when: k3s_int_check.rc != 0

    - name: Check if k3s-ext vnet already exists
      ansible.builtin.command: onevnet show k3s-ext
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      register: k3s_ext_check
      failed_when: false
      changed_when: false

    - name: Write k3s-ext vnet template (external, LAN bridge)
      ansible.builtin.copy:
        content: |
          NAME          = "k3s-ext"
          VN_MAD        = "bridge"
          BRIDGE        = "{{ one_ext_bridge }}"
          DNS           = "{{ one_dns_servers | join(' ') }}"
          GATEWAY       = "{{ one_ext_gateway }}"
          NETWORK_MASK  = "255.255.0.0"
          AR = [
            TYPE = "IP4",
            IP   = "{{ one_ext_ar_start }}",
            SIZE = "10"
          ]
        dest: /tmp/k3s-ext.one
        mode: '0644'
      when: k3s_ext_check.rc != 0

    - name: Create k3s-ext virtual network
      ansible.builtin.command: onevnet create /tmp/k3s-ext.one
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      when: k3s_ext_check.rc != 0

  post_tasks:
    - name: List OpenNebula hosts
      ansible.builtin.command: onehost list
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      register: onehost_list
      changed_when: false

    - name: Print onehost list
      ansible.builtin.debug:
        var: onehost_list.stdout_lines

    - name: List OpenNebula virtual networks
      ansible.builtin.command: onevnet list
      environment:
        ONE_AUTH: /var/lib/one/.one/one_auth
      register: onevnet_list
      changed_when: false

    - name: Print onevnet list
      ansible.builtin.debug:
        var: onevnet_list.stdout_lines

    - name: Print FireEdge URL
      ansible.builtin.debug:
        msg: "OpenNebula FireEdge UI: http://{{ homelab_ip }}:2616"
