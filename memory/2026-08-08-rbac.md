# RBAC Buildout (2026-08-08)

## Completed

### 1. LLDAP Groups
Created 10 new groups in LLDAP's SQLite DB (`/data/users.db`) and copied back to pod:
- auditors, service, dev, download, cms, monitor, security, gaming, 3dp, infrastructure
- Added yappingboy to all 10 groups via direct `memberships` table insert
- Restarted LLDAP pod to pick up changes

### 2. Keycloak Groups & Memberships
- Keycloak LDAP sync picked up all 16 groups (6 original + 10 new)
- Added all 10 new groups to yappingboy's `user_group_membership` table with `membership_type = 'STATIC'`
- Verified 10 memberships confirmed

### 3. oauth2-proxy Config Updates
**Admin proxy** (`oauth2-proxy.yaml`): added all 10 new groups to `allowed_groups`
**Media proxy** (`oauth2-proxy-media.yaml`): added download, cms, monitor, gaming, 3dp to `allowed_groups`

### 4. SSO Middleware Chains
Created `sso-chains.yaml` with 10 middleware chains:
- Each chain: `oauth2-redirect-{group}` → `keycloak-forwardauth-admin` → `{group}-role-check`
- Pattern: redirect on 401 → forwardAuth to oauth2-proxy → custom response headers

### 5. K3s RBAC ClusterRoles + Bindings
Created `cluster-roles-extended.yaml` with 8 ClusterRoles and 8 ClusterRoleBindings:
| Group | ClusterRole | Scope |
|-------|-------------|-------|
| /auditors | cluster-admin | Full cluster |
| /monitor | monitor-reader | Read-only monitoring ns |
| /gaming | gaming-writer | Read-write gaming ns |
| /3dp | 3dp-reader | Read-only 3dprinting + gridspace ns |
| /cms | cms-writer | Read-write cms ns |
| /security | security-reader | Read-only trivy-system + security ns |
| /infrastructure | infrastructure-writer | Read-write opennebula + traefik ns |
| /dev | dev-writer | Read-write toolbox + micro ns |

### 6. Commits
- `6d119ec` → pushed to main

## Pending
- Update existing IngressRoutes to use group-specific middlewares where needed
- Verify OAuth2 flow works for new groups through browser
- Add `service` group bindings (currently unbound - for service accounts)
