creation_rules:
  - path_regex: flux/.*\.yaml$
    # Replace with the output of: sudo age-keygen -y /root/.config/sops/age/homelab.agekey
    # (run on k3s-server, then commit this file)
    age: >-
      age1REPLACE_WITH_REAL_PUBLIC_KEY
    encrypted_regex: "^(data|stringData)$"
