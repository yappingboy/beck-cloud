# Kaniko build job — builds GridSpace grid-apps from upstream source and pushes to GHCR.
# Run once: kubectl apply -f kaniko-build.yaml -n toolbox

---
apiVersion: batch/v1
kind: Job
metadata:
  name: build-gridspace
  namespace: toolbox
spec:
  template:
    spec:
      serviceAccountName: kaniko-builder
      containers:
        - name: kaniko
          image: gcr.io/kaniko-project/executor:v1.24.0
          args:
            - --dockerfile=src/dock/Dockerfile
            - --context=https://github.com/GridSpace/grid-apps/archive/refs/tags/v2.0.4.tar.gz
            - --context-sub-path=v2.0.4
            - --destination=ghcr.io/yappingboy/becklab-gridspace:latest
            - --cache=true
            - --cache-repo=ghcr.io/yappingboy/kaniko-cache
            - --skip-tls-verify
          volumeMounts:
            - name: docker-config
              mountPath: /kaniko/.docker/
              readOnly: true
      restartPolicy: Never
      volumes:
        - name: docker-config
          secret:
            secretName: ghcr-credentials
